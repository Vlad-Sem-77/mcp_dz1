run = "python app.py"
entrypoint = "app.py"
modules = ["python-3.12"]

[nix]
channel = "stable-24_05"

[env]
PORT = "5000"

[deployment]
run = ["sh", "-c", "pip install -r requirements.txt && python app.py"]
deploymentTarget = "cloudrun"
